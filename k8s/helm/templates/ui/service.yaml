apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-store.fullname" . }}-ui
  namespace: {{ include "retail-store.namespace" . }}
  labels:
    {{- include "retail-store.ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type | quote }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-store.ui.selectorLabels" . | nindent 4 }}

