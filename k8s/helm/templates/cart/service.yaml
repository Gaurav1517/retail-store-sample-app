apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-store.fullname" . }}-cart
  namespace: {{ include "retail-store.namespace" . }}
  labels:
    {{- include "retail-store.cart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.cart.service.type | quote }}
  ports:
    - port: {{ .Values.cart.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-store.cart.selectorLabels" . | nindent 4 }}
