apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-store.fullname" . }}-checkout
  namespace: {{ include "retail-store.namespace" . }}
  labels:
    {{- include "retail-store.checkout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.checkout.service.type | quote }}
  ports:
    - port: {{ .Values.checkout.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-store.checkout.selectorLabels" . | nindent 4 }}
